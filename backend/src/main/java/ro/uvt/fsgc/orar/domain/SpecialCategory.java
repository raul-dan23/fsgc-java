package ro.uvt.fsgc.orar.domain;

/** Special activity category that may trigger reserved-interval block rules. */
public enum SpecialCategory {
    NORMAL,
    DPPD,
    CCOC,
    DCT,
    LIMBI_STRAINE
}