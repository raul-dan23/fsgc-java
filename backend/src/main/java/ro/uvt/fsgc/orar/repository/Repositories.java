package ro.uvt.fsgc.orar.repository;

/**
 * Marker for the repository package. Each Spring Data repository lives in its own
 * public interface file in this package so it can be injected from the service layer.
 */
public final class Repositories {
    private Repositories() {
    }
}
